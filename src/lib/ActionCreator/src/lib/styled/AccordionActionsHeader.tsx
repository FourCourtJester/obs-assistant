import styled from 'styled-components'

export const AccordionActionsHeader = styled.div`
  border: var(--bs-accordion-border-width) solid
    var(--bs-accordion-border-color);
  padding: 0 1.25rem;
`
